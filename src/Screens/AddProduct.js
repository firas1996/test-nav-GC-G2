import React from "react";
import { useOutletContext } from "react-router-dom";

const AddProduct = () => {
  const { id } = useOutletContext();
  useLoca;
  return <div>AddProduct Screen {id} </div>;
};

export default AddProduct;
